{
  "version": "0.2.0",
  "configurations": [
    {
      // run docker-compose up --watch --build
      "type": "msedge",
      "request": "launch",
      "name": "Debug nicobako.github.io",
      "url": "http://localhost:8080"
    }
  ]
}
